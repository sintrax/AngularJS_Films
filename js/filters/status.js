'use strict'

musicApp.filter('status', function(){
  return function(input) {
    switch (input) {
      case 1: return "Sold out"; brak;
      case 2: return "Released"; brak;
      case 3: return "Unreleased"; brak;

    }
  }
});
